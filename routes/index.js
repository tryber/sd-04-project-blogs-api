const authenticateRouter = require('./authenticateRouter');
const userRouter = require('./userRouter');
const postRouter = require('./postRouter');

module.exports = { authenticateRouter, userRouter, postRouter };
